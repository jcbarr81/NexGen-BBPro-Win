# Used when the Vite dev server is opened in a plain browser tab (no Electron
# shell). Electron injects these values via preload in the normal dev flow.
VITE_API_BASE_URL=http://127.0.0.1:8765
VITE_WS_BASE_URL=ws://127.0.0.1:8765
VITE_LAUNCH_TOKEN=
